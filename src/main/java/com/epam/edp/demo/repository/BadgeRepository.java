package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}apackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}cpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}kpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}apackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}gpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
} package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}cpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}mpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}.package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}apackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}mpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}.package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}dpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}.package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}dpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}mpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}.package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}spackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ipackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ypackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
};package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}
package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}
package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ipackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}mpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
} package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}cpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}mpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}.package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}apackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}mpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}.package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}dpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}.package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}dpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}mpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}.package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}npackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ipackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ypackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}.package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}Bpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}apackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}dpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}gpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
};package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}
package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ipackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}mpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
} package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}gpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}.package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}spackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ipackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}npackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}gpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}fpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}apackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}mpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}wpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}kpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}.package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}dpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}apackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}apackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}.package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}jpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}apackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}.package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}spackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ipackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ypackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}.package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}Jpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}apackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}Rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}spackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ipackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ypackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
};package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}
package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ipackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}mpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
} package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}gpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}.package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}spackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ipackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}npackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}gpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}fpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}apackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}mpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}wpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}kpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}.package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}spackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ypackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}.package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}Rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}spackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ipackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ypackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
};package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}
package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}
package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}/package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}*package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}*package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}
package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
} package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}*package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
} package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}Rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}spackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ipackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ypackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
} package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ipackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}npackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}fpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}apackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}cpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
} package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}fpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
} package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}Bpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}apackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}dpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}gpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
} package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}npackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ipackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ipackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}spackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}.package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}
package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
} package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}*package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}/package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}
package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}@package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}Rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}spackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ipackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ypackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}
package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}upackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}bpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}lpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ipackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}cpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
} package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ipackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}npackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}fpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}apackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}cpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
} package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}Bpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}apackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}dpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}gpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}Rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}spackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ipackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ypackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
} package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}xpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}npackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}dpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}spackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
} package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}Jpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}apackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}Rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ppackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}spackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ipackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}tpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}rpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}ypackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}<package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}Bpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}apackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}dpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}gpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}epackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
},package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
} package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}Lpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}opackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}npackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}gpackage com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}>package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
} package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}{package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}
package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}}package com.epam.edp.demo.repository;

import com.epam.edp.demo.entity.Badge;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for Badge entities.
 */
@Repository
public interface BadgeRepository extends JpaRepository<Badge, Long> {
}