# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.## Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.C# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.h# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.a# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.n# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.g# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.l# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.o# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.g# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.
# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.
# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.A# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.l# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.l# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.n# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.o# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.t# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.a# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.b# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.l# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.c# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.h# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.a# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.n# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.g# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.s# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.t# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.o# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.t# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.h# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.i# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.s# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.p# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.r# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.o# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.j# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.c# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.t# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.w# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.i# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.l# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.l# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.b# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.d# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.o# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.c# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.u# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.m# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.n# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.t# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.d# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.i# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.n# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.t# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.h# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.i# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.s# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.f# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.i# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.l# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file..# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.
# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.
# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.T# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.h# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.f# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.o# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.r# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.m# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.a# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.t# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.i# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.s# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.b# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.a# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.s# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.d# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.o# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.n# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.[# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.K# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.p# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.a# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.C# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.h# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.a# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.n# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.g# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.l# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.o# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.g# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.]# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.(# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.h# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.t# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.t# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.p# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.s# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.:# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file./# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file./# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.k# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.p# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.a# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.c# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.h# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.a# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.n# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.g# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.l# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.o# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.g# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file..# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.c# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.o# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.m# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file./# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.n# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file./# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.1# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file..# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.0# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file..# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.0# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file./# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.)# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.,# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.
# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.a# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.n# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.d# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.t# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.h# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.i# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.s# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.p# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.r# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.o# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.j# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.c# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.t# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.a# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.d# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.h# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.r# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.s# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.t# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.o# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.[# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.S# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.m# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.a# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.n# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.t# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.i# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.c# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.V# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.r# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.s# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.i# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.o# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.n# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.i# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.n# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.g# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.]# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.(# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.h# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.t# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.t# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.p# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.s# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.:# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file./# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file./# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.s# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.m# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.v# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.r# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file..# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.o# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.r# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.g# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file./# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.s# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.p# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.c# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file./# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.v# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.2# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file..# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.0# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file..# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.0# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file..# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.h# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.t# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.m# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.l# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.)# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file..# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.
# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.
# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.## Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.## Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.[# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.0# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file..# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.1# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file..# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.0# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.]# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.-# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.2# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.0# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.2# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.3# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.-# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.0# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.4# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.-# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.D# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.D# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.
# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.
# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.## Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.## Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.## Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.A# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.d# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.d# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.d# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.
# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.-# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.I# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.n# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.c# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.r# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.m# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.n# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.t# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.d# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.M# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.I# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.N# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.O# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.R# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.v# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.e# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.r# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.s# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.i# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.o# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.n# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.i# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.n# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.p# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.o# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.m# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file..# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.x# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.m# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.l# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.t# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.o# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file. # Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.0# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file..# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.1# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file..# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.0# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.-# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.S# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.N# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.A# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.P# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.S# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.H# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.O# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.T# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file..# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.
# Changelog

## [Unreleased]

### Added
- Entity class `Badge` with attributes and methods.
- Repository interface `BadgeRepository` extending `JpaRepository` for `Badge` entity.
- Service class `BadgeService` to interact with `BadgeRepository` and provide badge data.
- Controller class `BadgeController` with a GET endpoint to fetch all badges and CrossOrigin support.
- Updated `pom.xml` with dependencies for Spring Boot Web, JPA, H2 database, JAXB API, and org.javassist.
- Configuration for H2 database in `application.properties`.
- Preloaded data for `Badge` entity in `data.sql` file.